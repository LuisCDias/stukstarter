<%= simple_form_for(@project, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :short_description %>
  <%= f.input :description %>
  <%= f.input :image_url %>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
