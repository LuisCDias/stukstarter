<h1>Find Stukstarter Projects</h1>

<div class="row">
  <% @projects.each do |project| %>
    <div class="col-md-4">
      <%= link_to project.name, project_path(project) %>
    </div>
  <% end %>
</div>
